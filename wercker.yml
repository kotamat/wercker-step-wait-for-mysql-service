box: kotamat/laravel
services:
  - id: mysql
    env:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_USER: user
      MYSQL_PASSWORD: secret
      MYSQL_DATABASE: db
build:
  steps:
    - script:
      name: example
      code: |
        echo "Hello world"
